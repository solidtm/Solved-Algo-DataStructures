package com.solid.algolearning.kotlincode.algorithms.searching

internal class WeightedQuickUnionUF { //    The implementation for optimization is in the Union operation. The find operation(ie connected()) is same as the QuickFindUF class
    //    Here we modify the union method to check the sizes and link the root of the smaller tree to the root of the larger tree
    //    Then After changing the id length, we also change the id array size.
}